# SQL Server → PostgreSQL Migration Kit

[![CI](https://github.com/lubobali/mssql-to-postgres-migration-kit/actions/workflows/ci.yml/badge.svg)](https://github.com/lubobali/mssql-to-postgres-migration-kit/actions/workflows/ci.yml)

Moving a payments database off Microsoft SQL Server onto AWS-managed PostgreSQL,
and **proving the money still adds up on the other side.**

The problem this solves is the one that actually kills migrations. Not moving the data —
that part is easy. Knowing whether it arrived *correct*. A `MONEY` column silently losing
cents, or a timezone shifting settlement dates by a day, does not throw an error. It just
gives you wrong numbers, forever.

> **Status: in progress.** Building in the open. See [PLAN.md](PLAN.md) for the full scope
> and [docs/FINDINGS.md](docs/FINDINGS.md) for what has broken so far.

---

## What it does

```
     SOURCE                      MIGRATION                    TARGET
┌──────────────────┐      ┌────────────────────┐      ┌──────────────────────┐
│ SQL Server 2022  │      │  AWS DMS           │      │ RDS PostgreSQL 15    │
│ EC2, public sub  │─────▶│  full load + CDC   │─────▶│ private, encrypted   │
└──────────────────┘      └────────────────────┘      └──────────┬───────────┘
        │                                                        │
        │                 ┌────────────────────┐                 │
        └────────────────▶│  manual path       │────────────────▶│
                          │  extract → COPY    │                 │
                          └────────────────────┘                 │
                                                                 │
              ┌──────────────────────────────────────────────────┤
              │                                                  │
   ┌──────────▼──────────┐                        ┌──────────────▼─────────────┐
   │ VERIFICATION        │                        │ OPERATIONS                 │
   │ pytest, both sides  │                        │ snapshot → restore, timed  │
   │ counts, sums, dates │                        │ pg_cron scheduled jobs     │
   │ checksums, nulls    │                        │ Performance Insights       │
   │ FAILS on mismatch   │                        │ S3 Parquet → Snowflake     │
   └─────────────────────┘                        └────────────────────────────┘

   Every AWS resource is Terraform. Nothing is created by hand.
```

---

## Why the boring parts are the point

**Verification is a test suite, not a script.** Row counts, sum of every money column to the
cent, min/max on every date column, per-column NULL counts, per-merchant checksums, sampled
row hashes. It exits non-zero when anything differs, and it runs in CI. A verification script
that has never gone red is not a verification script — so the suite includes deliberately
planted failures to prove each check can catch its own case.

**The type mapping is where migrations actually break.** `MONEY` → `numeric(19,4)`, never
`float`, because rounding errors in payments are unacceptable. `DATETIME2` → `timestamptz`
with the timezone pinned explicitly, or settlement dates move by a day. And SQL Server's
default collation is case-**in**sensitive while PostgreSQL is case-sensitive, which changes
query results with no error at all — the most dangerous kind of migration bug.

**Some things have no equivalent on the other side.** RDS PostgreSQL has no SQL Server Agent,
so every scheduled job has to be rebuilt. There are no clustered indexes. Stored procedures
do not convert, they get rewritten. That list is a deliverable, not an afterthought.

---

## Security posture

| | |
|---|---|
| SSH | **No key pair, no port 22.** Access is SSM Session Manager |
| Database | Not publicly accessible. Reachable only from inside the VPC |
| Security groups | Postgres accepts **security group identities**, not IP ranges |
| Secrets | Generated by Terraform, stored in Secrets Manager, never in a file |
| IAM | Scoped to two specific secret ARNs, not `secretsmanager:*` |
| Storage | Encrypted at rest, both EBS and RDS |
| Instance metadata | IMDSv2 required |
| Data | **No real data anywhere.** Generated merchants, fake card last-4 |

---

## Stack

Terraform · AWS RDS PostgreSQL 15 · SQL Server 2022 (Docker) · AWS DMS ·
Secrets Manager · SSM Session Manager · Performance Insights · pg_cron ·
Alembic · Python · pytest · GitHub Actions

---

## Layout

```
terraform/     infrastructure — VPC, RDS, EC2, security groups, secrets
sqlserver/     legacy schema and data generator
migration/     extract and load, both the manual path and DMS config
verification/  the pytest suite that decides whether the migration passed
docs/          FINDINGS, RUNBOOK, and the RDS-vs-Aurora decision record
```

---

## Running it

```bash
cd terraform
cp terraform.tfvars.example terraform.tfvars   # set your own IP
terraform init
terraform apply
```

Tear it down completely:

```bash
terraform destroy
```

Everything is tagged `project=rds-migration-lab`, so nothing is left behind.

**Cost while running: roughly $1.50/day.**
